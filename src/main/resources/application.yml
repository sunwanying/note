server:
  port: 8080
spring:
  application:
    name: front

  profiles:
#    active: prod
    active: dev

  mvc:
    favicon:
      enabled: false

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: cn.sunguolei.note.domain
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    cn.sunguolei.note: debug